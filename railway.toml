[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/backend/Dockerfile"

[build.buildArgs]
CACHE_BUST = "20260618-1"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
